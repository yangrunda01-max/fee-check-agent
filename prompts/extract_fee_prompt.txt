Extract international tuition fee from university webpage text below.

## CRITICAL
1. Extract ONLY from the provided text. Do NOT use external knowledge or training data.
2. If text has no explicit international tuition fee, set confidence="none".
3. Do NOT guess, infer, or speculate fee amounts.

## Rules
- Extract only fees labeled as international student fees. NOT domestic/CSP/home.
- For multiple academic years, use the most recent.
- Distinguish: per_year | total | per_semester | per_unit.
- If page only has a fee calculator with no amount, confidence="none".

## STRICT — Only these 9 fields allowed
NO fee_text, year, source_url, explanation, reasoning, markdown, code blocks, backticks, or any extra fields.

{
    "amount": "numeric digits only, or empty string",
    "currency": "3-letter code or empty",
    "unit": "per_year | total | per_semester | per_unit | empty",
    "confidence": "high | medium | low | none",
    "is_international": true | false,
    "is_domestic": true | false,
    "fee_type": "international | domestic | empty",
    "source_quote": "max 60 chars, short verbatim quote, or empty string",
    "note": "max 80 chars brief note, or empty string"
}

## Limits
- source_quote: MAX 60 chars. Short quote, NOT full paragraph.
- note: MAX 80 chars. Empty string if no remark.
- Do NOT copy entire page text into note.

## Confidence
- high: fee clearly displayed, explicitly international, amount+currency visible
- medium: fee visible but context ambiguous (year/unit unclear)
- low: fee mentioned but ambiguous
- none: No international fee found. Safest when uncertain.

## Output
Return ONLY a single valid JSON object. No markdown, no ```code blocks```, no backticks, no extra text.